@model Product

<div class="card border-success mb-3 p-1">
    <div class="card-body">
        <h4 class="card-title">
            @Model.Name
            <span class="badge rounded-pill bg-primary text-white" style="float: right">
                <small>@Model.Price.ToString("c")</small>
            </span>
        </h4>
        <div class="card-text">
            <div class="card-text p-1">@Model.Description</div>
        </div>
    </div>
</div>